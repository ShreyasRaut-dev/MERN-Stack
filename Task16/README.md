# Task: (JS) Map filter Exercise

Objective: The objective of this assignment is to create a web page that displays student records using HTML and JavaScript. The web page should include some cards showing student details such as name, marks, class, and address. Additionally, a search box should be implemented to filter students by name dynamically.

Tasks:
Create an HTML file that includes the following elements:

Create an array of student objects with the following attributes:

Name

Marks

Class

Address

Render the student records in card format as shown in the sample output using HTML.

Implement a search box that filters students by name dynamically using JavaScript's filter function.

Instructions:

Create an array of student objects, each containing attributes for name, marks, class, and address.

Implement HTML markup to display a card format as shown in sample output for student records.

Populate the cards with the student records retrieved from the array.

Implement JavaScript code to handle the following:

Rendering student records dynamically in the cards.

Implementing a search functionality that filters students by name as the user types in the search box.

Ensure that the search functionality updates the displayed student records in real-time based on the entered search text.

Use Map Function to populate the entries And Filter function to get students with searched names.

UI Details based on visual reference:

Top header containing "Hi, Username" on the left and a "Login" button on the right.

A search bar with a "Search" button next to it.

Below the search bar, a heading saying "Search Results for..."

A grid of cards. Example data to include in the array:

Riya Sharma, Marks: 85%, Class: 10th, Address: 123, ABC Colony, Delhi

Rohan Patel, Marks: 70%, Class: 12th, Address: 456, XYZ Street, Mumbai

Rajesh Singhania, Marks: 92%, Class: 9th, Address: 555, VWX Street, Jaipur
