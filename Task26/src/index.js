import React from 'react';
import ReactDOM from 'react-dom/client';
import HeloWrld from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <HeloWrld />
  </React.StrictMode>
);
